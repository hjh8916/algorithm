const fs = require('fs');

// TODO
// 1. 입력값 설정
// 2. solution parm 설정

const input = fs.readFileSync(`${__dirname}/data.txt`).toString();


const solution = () => {
   
}

const result = solution(n, arr);
console.log(result);



